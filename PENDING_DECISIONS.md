# 需用户确认的决策列表

这个文件列出所有触碰项目核心边界（PROJECT_GUARDRAILS.md），需要你确认才能继续。

---

## 决策 1：接入真实赔率数据源

**问题：** 当前 2026 数据只是 mock/fallback，无法提供有意义的分析。

**证据：** `data/matches-2026.json 只有 1 条占位数据，引擎对 2026 的风控评分没有意义。

**建议：** 接入一个稳定的赔率/盘口数据源，候选包括：
- **The Odds API**：免费额度约 500 请求/月（胜平负/让球/大小球有数据）
- **API-Football**：赛程、赛果、阵容信息
- **Sportmonks**：更多结构化数据

接入方式：通过 GitHub Actions 定期抓取 → 生成 data/matches-2026.json，页面 JSON 优先加载。

**好处：** 2026 世界杯期间可以自动更新赔率/盘口/波胆/盘口分析，提供实时风控分析。

**风险：** 需要 API Key（免费额度是否足够）；GitHub Pages 静态部署不受影响，但 Action 可以正常运行）；某些数据源条款是否允许自动化抓取；需要处理汇率/时区/赔率归一化。

**当前状态：调研中 — **需要你确认**。

---

## 决策 2：是否把比分扩展策略纳入正式模型

**问题：** 当前比分覆盖优化雏形（engine/score-expander.js）只作为模拟模块存在，不影响正式推荐。

**证据：** 模拟结果在跨届汇总看板展示“比分覆盖扩展后，2022 年约 66% 命中率，但尚未经过你确认才可以改正式模型。

**建议：** 暂不做。待 2026 开赛后用真实数据再评估。

**好处：** 暂不改动正式模型。

**风险：** 如果直接替换正式模型可能引入副作用（候选可能导致过拟合历史数据的风险。

**当前状态：模拟验证中 — **暂保留模拟模块，等你确认是否合并。

---

## 决策 3：是否重构风控评分权重

**问题：** 当前评分权重（engine/config.js）是第一版经验规则，不是优化过拟合历史样本优化。

**证据：** 还没有做过权重的调优/对比；调整可能提升命中率调优调优/对比的权重是否需要确认。

**建议：** 等 2026 有真实数据再做权重调优提案，先保留当前权重。

**好处：** 有新数据做合理调优更有意义。

**风险：** 当前样本上做权重调优可能导致过拟合/不稳定。

**当前状态：** — **需要你确认后再做**。

---

## 决策 4：是否引入 AI 项目上线

**问题：** AI 模型是否替换为 AI 模型

**建议：先不提这个项目** — **暂不做，优先做评分引擎不变。

---

## 决策 5：是否把 preview.html 升级为正式入口 index.html

**问题：** 当前正式页 index.html 是稳定版单文件架构，preview.html 包含多视图导航和自动数据源架构。需要你确认何时把测试版升级为正式入口。

**证据：**
- preview.html 今日比赛页 + 数据完整度评分 + 自动数据源架构已经可用（23/23 功能点通过
- GitHub Pages 正式页 index.html 仍在稳定版
- 自动数据源仍在 mock/fallback 模式，尚未接入真实 API

**建议：** 等 FIFA 公开 2026 赛程、你确认 API Key 后，把 preview.html 替换为正式入口。

**好处：** 无需停机部署，你在手机上可以直接用 preview.html 测试真实数据。

**风险：** 替换正式 index.html 前，需要你确认 2026 真实数据接入并通过一次完整运行验证。

**当前状态：调研中 — **需要你确认**。

---

## 决策 6：GitHub Actions 是否改为定时触发（自动更新 JSON）

**问题：** 当前 workflow 只有手动触发。如果后续接入真实数据源，可能需要定时更新 data/matches-2026.json / data/source-status.json。

**证据：** 目前 2026 世界杯未开赛，数据源也没有 API Key。

**建议：** 等 FIFA 赛程公开 + API Key 配置后，再开启定时触发。

**当前状态：保留手动触发 — **需要你确认后再改。
